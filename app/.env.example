# Set to "true" only when Wrangler dev server is running on port 8788
# When "false", all /api/* calls use the localStorage mock database
VITE_USE_WRANGLER=false

# File upload auth is handled server-side (origin guard) — no client API key needed.